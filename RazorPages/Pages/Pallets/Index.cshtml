@page
@model RazorPages.Pages.Pallets.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Pallet[0].Product)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Pallet[0].Amount)
        </th>
        <th></th>
    </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Pallet) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Product.Name)
            , 
            @Html.DisplayFor(modelItem => item.Product.Type)
            , 
            @Html.DisplayFor(modelItem => item.Product.PackingType)
            , 
            @Html.DisplayFor(modelItem => item.Product.Volume)
            L.
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Amount)
        </td>
        <td>
            <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
            <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
            <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
        </td>
    </tr>
}
    </tbody>
</table>
