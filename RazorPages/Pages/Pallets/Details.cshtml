@page
@model RazorPages.Pages.Pallets.DetailsModel

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Pallet</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Pallet.Amount)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Pallet.Amount)
        </dd>
    </dl>
</div>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.Pallet?.Id">Edit</a> |
    <a asp-page="./Index">Back to List</a>
</div>
