@page
@model RazorPages.Pages.Deliveries.DetailsModel

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Delivery</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Delivery.Date)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Delivery.Date)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Delivery.UserName)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Delivery.UserName)
        </dd>
        <dt class="col-sm-2">
            Pallets list:
        </dt>
        <dd class="col-sm-10">
            @foreach (var item in Model.Pallets)
            {
                <br />
                @Html.DisplayFor(modelItem => item)
            }
        </dd>
    </dl>
</div>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.Delivery?.Id">Edit</a> |
    <a asp-page="./Index">Back to List</a>
</div>
